from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Hotel data will provide insight into customer segments and lifetime value.',
    author='Lindsey Robertson',
    license='',
)
